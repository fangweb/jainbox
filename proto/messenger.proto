syntax = "proto3";

package pb;

service Messenger {
  rpc Notify (NotifyMessage) returns (NotifyResponse) {}
}

message NotifyMessage {
  string Username = 1;
  string Type = 2;
  string Notification = 3;
}

message NotifyResponse {
  bool Notified = 1;
}
